var shouldCheck = true;
loop()
{

	if (shouldCheck)
		checkMail();
	else


	if mail = true || time > 12 hours






	check for mail
		if mail; notification; supress unil change state;

	if no mail; start checking for mail again

}

var maxBrightness = max; // Minimum brightness to trip sensor
checkMail()
{
	wait 1 // setTimeout(1);
	
	lightsOn();
	var lightValue = readLightSensor();
	lightsOff();

	if (lightValue < maxBrightness)
		youveGotMail = true;
	elseif (lightValue >= maxBrightness)
		youveGotMail = false;

	shouldCheck=false;
}